# 🏛️ DCW-SETIF — دليل الوكيل والمطور (AGENTS.md)
### منصة الرقابة والتفتيش الميداني — مديرية التجارة الداخلية وضبط السوق الوطنية لولاية سطيف

> **تاريخ آخر تحديث**: 20 سبتمبر 2026  
> **حالة المنظومة**: تم تطبيق المنظومة الإمنية والإدارية الصارمة 100% (ربط الأجهزة، القفل الجغرافي الصارم للمقرات الـ 8، نوافذ التوقيت القانونية، إثبات الانصراف الميداني، إزالة التجاوزات الوهمية)  
> **مستودع التطبيق (Flutter Web & Mobile)**: [DCW-SETIF-TRACKER](https://github.com/matique2026ai-ux/DCW-SETIF-TRACKER) (الفرع الحالي: `main`)  
> **مستودع الخادم (Node.js/Express/PostgreSQL)**: [DCW-SETIF-BACKEND](https://github.com/matique2026ai-ux/DCW-SETIF-BACKEND) (الفرع الحالي: `main`)  
> **رابط المنصة الحية (Render Web App)**: [dcw-setif-tracker.onrender.com](https://dcw-setif-tracker.onrender.com)  
> **السيرفر السحابي الحي (Render Backend API)**: `https://drh-setif-api.onrender.com/api`  
> **معرف النشر الأخير على Render**: `dep-danou6v40ujc73cm2e90`

---

## 📌 ما تم إنجازه وتطبيقه في جلسة 20 سبتمبر 2026 (المنظومة الأمنية والإدارية الصارمة):

1. **🔒 ربط الحساب بجهاز الهاتف المعتمد (Device Security & Anti-Spoofing):**
   * إضافة حقلي `DeviceId` و `DeviceName` في قاعدة البيانات لجدول المستخدمين و `TrackerAttendance`.
   * التوثيق التلقائي للهاتف عند تسجيل الدخول الأول، ورفض الدخول فوراً من جهاز ثانٍ أو متصفح مجهول (HTTP 403) مع إتاحة إعادة التهيئة لمدير النظام التقني.

2. **📍 القفل الصارم للبصمة الجغرافية (Strict GPS Geofence للمقرات الرسمية الـ 8):**
   * اعتماد الإحداثيات الرسمية الدقيقة وأنصاف الأقطار بالـ Haversine لكافة المقرات (المقر الرئيسي بالمعبودة، مطار 8 ماي 1945، مفتشيات العلمة، عين ولمان، بوقاعة، وملحقات عين آزال، عين الكبيرة، عين أرنات).
   * رفض تسجيل الحضور الصباحي قطعياً خارج النطاق الجغرافي، مع حظر التخفي خلف الـ Offline الوهمي عند حدوث رفض أمني من السيرفر.

3. **⏰ نوافذ توقيت الدوام الرسمي والمداومة الليلية (Time Windows):**
   * حصر تسجيل الحضور الصباحي بين 07:30 و 10:30 صباحاً بتوقيت الجزائر (UTC+1).
   * إقفال تسجيل الحضور المسائي تلقائياً واشتراط أمر المهمة الليلية / المداومة المؤشرة في النظام مسبقاً.

4. **📋 ضوابط الانصراف الميداني وإثبات النشاط (Checkout Validation & Proof):**
   * منع الانصراف الفوري (أقل من 30 دقيقة) دون تقديم تبرير استعجالي رسمي.
   * اشتراط إتمام نصف الدوام (4 ساعات) أو تسجيل معاينات ميدانية مع التاجر قبل الانصراف، وإلزام المفتش بتقديم مبرر إداري عند الخروج المبكر.
   * توثيق إحداثيات موقع إنهاء المهمة الميدانية وإرفاق إحصائيات المعاينات في بطاقة الإثبات الرقمي (QR Code).
   * تحقيق **0 Errors / 0 Warnings** بالكامل في `flutter analyze`.

---

## ⚠️ تعليمات صارمة للوكلاء والمطورين اللاحقين (STRICT AGENT DIRECTIVES)

1. **🚫 منع توليد حزمة الأندرويد APK تماماً:**
   * **ممنوع قطعياً تشغيل `flutter build apk` أو توليد أي ملف `.apk` إلا بأمر كتابي صريح ومباشر من المستخدم** (بناءً على التوجيه الصريح: *"لا تولد تطبيق ابيكي فقط حتى امرك"*).
2. **🛡️ حساب مدير النظام (الأدمن) ممنوع اللمس:**
   * الحساب `tracker_admin` (Role 5) **ممنوع اللمس نهائياً**، لا يتم حذفه ولا تعديل بياناته ولا تفريغه.
3. **🌿 إدارة الفروع والنشر:**
   * التحديثات تم دمجها بالكامل في فرع `main` لكلا المستودعين، ويتم إكمال التطوير مباشرة على `main` أو وفق توجيه المستخدم.
   * خادم Render متصل بفرع `main` وتحديثات `main` تنعكس تلقائياً عبر Render Webhooks و Deploy Hooks.
4. **🔍 معايير الجودة والاستقرار:**
   * الحفاظ دائماً على **`0 Errors / 0 Warnings`** في `flutter analyze` و `dart analyze`.
   * عدم استخدام أي إرجاع وهمي (`fake mock success`) في الواجهات؛ كل زر يجب أن يرتبط بمسار حقيقي وقاعدة بيانات حقيقية.

---

## 📌 ما تم إنجازه وإصلاحه في جلسة 19 سبتمبر 2026:

1. **ظهور وإبراز دور رئيس مصلحة الإدارة والوسائل:**
   * تفعيل شاشة ودور **رئيس مصلحة الإدارة والوسائل** (`chef_administration`) لمتابعة الحضور، الانضباط، التأخرات، المبررات، ومقترحات الاقتطاع من الراتب وفق الأمر 06-03.
   * إضافة **قائمة تبديل المصالح الفورية (`Department Switcher`)** في شاشة رئيس المصلحة (`HeadScreen`) للتنقل بين المصالح الثلاث:
     * مصلحة الإدارة والوسائل.
     * مصلحة حماية المستهلك وقمع الغش.
     * مصلحة المنافسة والتحقيقات الاقتصادية.
   * إصلاح خطأ قراءة معرف الموظف في `HeadScreen` (`mapLookup`) ليتم جلب حالة الموظفين الحية بدقة دون `null`.

2. **القضاء على الأكواد والاستجابات الوهمية (Mock Returns):**
   * إزالة كافة استجابات `catch (_) { return {'success': true}; }` في خدمة `api_service.dart`.
   * ربط أزرار الأدمن (تصفير البيانات، تغيير فترة السماح الصباحية، توليد الحسابات) بمسارات السيرفر الحقيقية مع إرجاع التنبيهات والأخطاء الواقعية.

3. **إصلاح تناقضات خريطة المدير والبث الحي (`DirectorMapTab`):**
   * ربط عداد وبطاقة "في مهمة تفتيشية ميدانية" بالزيارات الميدانية والتواجد خارج محيط المقر.
   * إبراز برنامج الرقابة النشط ورقم أمر المهمة في بطاقة تفاصيل المفتش.
   * ضبط مؤشرات العطل الرسمية الأسبوعية (الجمعة والسبت) لمنع تصنيف الموظفين كغائبين.

4. **تصفير وتفريغ قاعدة البيانات بالكامل (Clean Slate):**
   * مسح كافة بيانات التجارب والحضور والزيارات: `TrackerAttendance`, `TrackerVisits`, `TrackerDeductions`, `TrackerAbsences`, `TrackerPrograms` بـ `TRUNCATE ... RESTART IDENTITY CASCADE`.
   * تفريغ جدول الموظفين `Employes` تماماً (0 موظف)، وحذف حسابات `emp.%`، ليصبح التطبيق فارغاً ونظيفاً تماماً وجاهزاً للإنتاج من الصفر.
   * الحفاظ التام والآمن على حساب الأدمن وحسابات مدراء الشاشات.

---

## 🔑 الحسابات المعتمدة النشطة في قاعدة البيانات الحالية:

| المستخدم (Username) | كلمة المرور (Password) | الدور (Role) | الصلاحية والواجهة |
| :--- | :--- | :--- | :--- |
| **`tracker_admin`** | `admin123` | `admin` (Role 5) | **مدير النظام التقني (محفوظ 100% — ممنوع اللمس)** |
| **`directeur`** | `directeur123` | `director` (Role 1) | **المدير الولائي (الآمر بالصرف)** |
| **`chef_administration`** | `chef123` | `head_of_department` (Role 2) | **رئيس مصلحة الإدارة والوسائل** |
| **`chef_consommation`** | `chef123` | `head_of_department` (Role 2) | **رئيس مصلحة حماية المستهلك وقمع الغش** |
| **`chef_concurrence`** | `chef123` | `head_of_department` (Role 2) | **رئيس مصلحة المنافسة والتحقيقات الاقتصادية** |
| **`bureau_user`** / **`chef_bureau`** | `bureau123` | `bureau_chief` (Role 3) | **رئيس مكتب المستخدمين** |
| **`inspecteur`** | `chef123` | `inspector` (Role 4) | **حساب مفتش رقابة تجريبي لاختبار شاشة المفتش** |

---

## 🔄 مسار العمل المترابط الحقيقي (Integrated Live Workflow):

```
1. المفتش الميداني (Inspecteur):
   └─► يسجل حضوره الصباحي بالـ GPS ──► يبدأ تنفيذ أمر مهمة رقابية ويسجل زيارة تفتيشية مع التاجر بالصور.
2. رئيس المصلحة المعنية (قمع الغش / المنافسة):
   └─► يرى الزيارة فورياً في شاشته ──► يقوم بتأشير ومصادقة محضر المعاينة، عينات التحليل، أو المحجوزات.
3. رئيس مصلحة الإدارة والوسائل (Administration & Moyens):
   └─► يتابع الانضباط العام، التأخرات التراكمية، تبريرات الغياب، ومقترحات الخصم من الراتب.
4. المدير الولائي (Directeur - Ordonnateur):
   └─► يتابع في الخريطة الحية مواقع الفرق، والزيارات المنجزة، ويفصل سيادياً في ملفات الاستفسارات والخصم.
```

---

## 🌐 روابط وخطافات النشر السحابي (Render Deploy Hooks):

* **رابط إعادة النشر المباشر لخادم Render (Deploy Hook):**
  ```powershell
  curl.exe -X POST "https://api.render.com/deploy/srv-dairbhdg1s2s738fig10?key=GLgWofYjMBE"
  ```
* **رابط فحص الحالة والصحة الحية:**
  `https://drh-setif-api.onrender.com/api/health`
