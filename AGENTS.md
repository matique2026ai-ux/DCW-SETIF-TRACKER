# 🏛️ DCW-SETIF — دليل الوكيل والمطور (AGENTS.md)

## منصة الرقابة والتفتيش الميداني — مديرية التجارة الداخلية وضبط السوق الوطنية لولاية سطيف

> **تاريخ آخر تحديث**: 14 سبتمبر 2026  
> **حالة المنظومة**: جاهزة للإنتاج 100% (Production Ready)  
> **مستودع التطبيق (Flutter)**: [DCW-SETIF-TRACKER](https://github.com/matique2026ai-ux/DCW-SETIF-TRACKER)  
> **مستودع الخادم (Node.js/Express)**: [DCW-SETIF-BACKEND](https://github.com/matique2026ai-ux/DCW-SETIF-BACKEND)  
> **السيرفر السحابي الحي**: `https://drh-setif-api.onrender.com/api`  
> **حزمة الأندرويد الرسمية**: [DCW-SETIF-TRACKER.apk](./DCW-SETIF-TRACKER.apk) (~65 MB)

---

## 📌 القواعد الإلزامية للوكلاء والمطورين (Developer Guidelines)

1. **الترابط المنطقي بين الأدوار**: كل ميزة برمجية يجب أن تحترم التدرج الوظيفي الإداري (Admin -> Director -> Head of Department -> Bureau Chief / Head of Inspectorate -> Inspector).
2. **منظومة المقرات المتعددة (Multi-HQ & Regional Inspectorates)**:
   * يشمل النظام **المقر الرئيسي لمديرية سطيف (المعبودة)** و**المفتشية الحدودية لمراقبة الجودة بمطار 8 ماي 1945 (عين أرنات)** و**المفتشيات الإقليمية (العلمة، عين ولمان، بوقاعة)** و**الملحقات التجارية الثلاث (عين آزال، عين الكبيرة، عين أرنات)**.
   * يتعرف التطبيق تلقائياً عبر البصمة الجغرافية GPS على المقر/المفتشية/الملحقة/المطار الذي التحق به العون أو المفتش ويوثق الحضور باسم المقر/الملحقة مباشرة.
3. **حماية الخصوصية ومنطق الحضور والانصراف الميداني**:
   * **إلغاء طلب السيلفي/تصوير الوجه نهائياً**.
   * **تسجيل الحضور الصباحي (Check-In)**:
     * *في المقر أو المفتشية*: بلمسة واحدة عبر البصمة الجغرافية الذكية (GPS Geofence) + توليد بطاقة إثبات رقمية مشفرة بـ QR Code يمكن للمدير أو رئيس المصلحة فحصها وتأكيدها.
     * *في حال الانطلاق المباشر في مهمة رقابية (سوق جملة، مذابح، بلدية نائية)*: تسجيل الحضور الميداني المباشر مع توثيق سبب المهمة والموقع الفعلي.
   * **تسجيل الانصراف المسائي (Check-Out)**: مرن وميداني؛ يسجل المفتش انصرافه مباشرة من المكان الذي وصل إليه وأنهى فيه مهامه الرقابية (أو من المقر) دون إلزامه بالرجوع، مع توثيق إحداثيات الموقع وتفعيل حجب التتبع فوراً لحماية خصوصية الموظف خارج أوقات العمل.
4. **توثيق المعاينات الميدانية للمحلات**:
   * الكاميرا مخصصة حصراً لتوثيق النشاط التجاري (الواجهة، الأسعار، المحاضر، المخالفات).
5. **المزامنة دون اتصال (Offline-First)**:
   * كافة العمليات تحفظ محلياً عبر `OfflineSyncService` في حال انعدام الشبكة وتتم مزامنتها تلقائياً.
6. **تحديث ملفات التوثيق والـ Git فوراً**:
   * كل تعديل جوهري يجب أن يتبعه تحديث ملفات التوثيق `README.md` و `AGENTS.md`، وبناء الحزم ورفعها مباشرة إلى مستودعات GitHub.
7. **بطاقة الـ QR الرقمية للمفتش الميداني**:
   * بيانات رمز الـ QR والبطاقة الرقمية (`QRCodeScreen`) تظهر بصيغة نصية عربية رسمية وواضحة (اسم المفتش، الرتبة، المصلحة، وتأكيد الهوية الرقمية الرسمية) عند مسحها بأي كاميرا هاتف عادية بدون الحاجة إلى تطبيق خاص أو إظهار كود JSON.
8. **هوية تطبيق الأندرويد والأيقونة الرسمية**:
   * اسم التطبيق في `AndroidManifest.xml` هو **`مديرية التجارة سطيف`**.
   * الأيقونة الرسمية للتطبيق في `res/mipmap` هي شعار العملة الذهبية (`gold_coin_floating.png`) بجميع مقاسات الشاشات.
   * ذاكرة بناء الـ Gradle في `gradle.properties` مضبوطة بـ `-Xmx2048m` لضمان استقرار البناء في مختلف الأجهزة دون انهيار JVM.

---

## 🔑 الحسابات الرسمية والصلاحيات (Official Credentials)

| المستخدم (Username) | كلمة المرور (Password) | الدور (Role) | الصلاحية والوصف |
| :--- | :--- | :--- | :--- |
| `tracker_admin` | `admin123` | `admin` | **مدير النظام التقني**: إدارة الحسابات، توليد البيانات للموظفين، ضبط الإعدادات |
| `directeur` | `directeur123` | `director` | **المدير الولائي**: لوحة القيادة، خريطة الولاية والمفتشيات الإقليمية، تصدير تقارير PDF |
| `chef_concurrence` | `chef123` | `head_of_department` | **رئيس مصلحة المنافسة والتحقيقات الاقتصادية**: أوامر المهمة، اعتماد المعاينات |
| `chef_consommation` | `chef123` | `head_of_department` | **رئيس مصلحة حماية المستهلك وقمع الغش**: توجيه الفرق الرقابية، متابعة المحاضر |
| `bureau_user` | `bureau123` | `bureau_chief` | **رئيس مكتب المستخدمين**: متابعة الفرق الميدانية وتوزيع التكليفات |
| `kriba` | `Agent@2024` | `inspector` | **مفتش رئيسي (كريبع كمال)**: تسجيل حضور جغرافي ذكي، تنفيذ المهام والمعاينات |
| `agent` | `Agent@2024` | `inspector` | **مفتش ميداني عام**: تسجيل حضور، إنجاز زيارات، دعم وضع عدم الاتصال |

---

## 🔄 مسار العمل الميداني (Inspection Workflow)

```text
1. التحاق المفتش بالمقر أو المفتشية الإقليمية (أو انطلاق ميداني مباشر بمهمة)
   └─► بصمة جغرافية GPS ذكية + بطاقة إثبات رقمية QR Pass ──► تسجيل الحضور
2. استلام أمر المهمة الرقابية
   └─► رئيس المصلحة / رئيس المفتشية ينشئ برنامج الرقابة ──► يظهر تلقائياً في شاشة المفتش
3. المعاينة الميدانية للمحل التجاري
   └─► المفتش يدخل بيانات المحل + تصوير المخالفة/السلعة ──► إرسال فوري للسيرفر
4. التأشير والاعتماد الرسمي
   └─► رئيس المصلحة يراجع التقرير ويعتمده ──► يظهر في لوحة المدير وتقارير الـ PDF
5. الانصراف الميداني المرن
   └─► تسجيل الانصراف من موقع انتهاء المعاينات ──► إيقاف وحجب التتبع لحماية الخصوصية
```

---

## 🛠️ أوامر الصيانة والبناء (Build Commands)

```powershell
# بناء تطبيق الأندرويد للإنتاج:
cd "c:\Users\PCIB\Desktop\Nouveau dossier\drh_setif_tracker"
flutter build apk --release

# بناء تطبيق الويب:
flutter build web --release

# فحص سلامة كود فلاتر:
flutter analyze

# دفع التحديثات إلى GitHub:
git add .
git commit -m "feat/docs: description"
git push origin main
```
