# 🏛️ DCW-SETIF — دليل الوكيل والمطور (AGENTS.md)
### منصة الرقابة والتفتيش الميداني — مديرية التجارة الداخلية وضبط السوق الوطنية لولاية سطيف

> **تاريخ آخر تحديث**: 16 سبتمبر 2026  
> **حالة المنظومة**: جاهزة للإنتاج والعرض الوزاري 100% (Production & Ministry Presentation Ready)  
> **مستودع التطبيق (Flutter Web & Mobile)**: [DCW-SETIF-TRACKER](https://github.com/matique2026ai-ux/DCW-SETIF-TRACKER)  
> **مستودع الخادم (Node.js/Express/PostgreSQL)**: [DCW-SETIF-BACKEND](https://github.com/matique2026ai-ux/DCW-SETIF-BACKEND)  
> **رابط المنصة الحية (Render Web App)**: [dcw-setif-tracker.onrender.com](https://dcw-setif-tracker.onrender.com)  
> **السيرفر السحابي الحي (Render Backend API)**: `https://drh-setif-api.onrender.com/api`  
> **حزمة الأندرويد الرسمية**: [DCW-SETIF-TRACKER.apk](./DCW-SETIF-TRACKER.apk) (~67.4 MB)

---

## 📌 القواعد الإلزامية والمنطق الإداري المعتمد (Developer & Business Rules)

### 1. السلطة السيادية للمدير الولائي (الآمر بالصرف - Ordonnateur)
* **المدير الولائي هو صاحب القرار النهائي والآمر بالصرف الوحيد في المنظومة**.
* **مكتب المستخدمين (`BureauScreen`) لا يملك أي سلطة لرفض أو تجميد قرارات المدير**:
  * دور مكتب المستخدمين ينحصر في متابعة سجل التأخرات، توجيه الاستفسارات، واقتراح الحسابات، وتنفيذ قرارات الخصم الصادرة عن المدير فوراً على كشف الراتب (`Exécuté sur Paie`).
* تم مسح وإلغاء أي منطق قديم كان يسمح باعتراض مكتب المستخدمين على قرارات المدير.

### 2. مطابقة قانون الوظيفة العمومية الجزائري (الأمر رقم 06-03)
* تطبيق مبدأ **"الراتب مقابل أداء الخدمة" (Service Fait)**.
* احتساب التأخر التراكمي وتجميعه شهرياً وفق المعادلة القانونية الرسمية:
  * **تراكم 4 ساعات تأخر شهرياً = خصم نصف يوم عمل (0.5 يوم)**.
  * **تراكم 8 ساعات تأخر شهرياً = خصم يوم كامل (1.0 يوم)**.
* إلغاء الخصم المباشر العشوائي، واعتماد مسار الانضباط التدرجي القانوني.

### 3. فترة التسامح الصباحية القابلة للضبط (Dynamic Morning Grace Tolerance)
* **إلغاء المقارنة الإلزامية الصارمة بتوقيت 08:00 صباحاً**.
* توفير خيار ضبط ديناميكي لفترة التسامح الصباحية متاح حصراً في شاشتي **المدير الولائي** و**مدير النظام (Admin)**:
  * الخيارات المعتمدة: `08:15`، `08:30`، `08:45` (الافتراضية الموصى بها)، `09:00` (مرونة قصوى)، `09:15`.
  * الحضور قبل توقيت التسامح المختار يعتبر انضباطاً كاملاً (تأخر = 0 دقيقة)، ويحسب التأخر فقط لما زاد عن العتبة المعتمدة.

### 4. مسار الاستفسار الإداري وحق الدفاع (48h Inquiry Lifecycle)
* **استمارة الاستفسار الرسمية**: توليد استمارة إدارية مطبوعة مطابقة للنماذج الرسمية للجمهورية الجزائرية الديمقراطية الشعبية (`InquiryLetterDialog`).
* **إشعار ومهلة 48 ساعة**: يتلقى المفتش إشعاراً في حسابه لتقديم تبريراته المكتوبة عبر التطبيق (`InspectorInquiriesSheet`).
* **الملف المتكامل أمام المدير**: تعرض تبريرات الموظف مع 4 قرائن إثبات رقمية (بصمة GPS، توقيت الحضور بالنسبة لفترة التسامح، عدد الزيارات الميدانية المنجزة، وحالة أمر المهمة).
* **الخيارات السيادية الثلاثة للمدير**:
  1. `justified`: قبول التبرير وحفظ الملف دون أي أثر مالي.
  2. `warning`: توجيه تنبيه إداري يسجل في الملف المهني.
  3. `deduction_ordered`: إصدار قرار خصم نافذ ومحدد بالأيام (0.5، 1، 2 يوم) يحال آلياً لمكتب المستخدمين للتنفيذ على الراتب.

### 5. منظومة المقرات المتعددة والبصمة الجغرافية (Multi-HQ GPS)
* التعرف التلقائي الذكي على المقر دون حاجة للاختيار اليدوي:
  1. **المقر الرئيسي لمديرية سطيف (حي المعبودة)** (36.1912, 5.4137).
  2. **المفتشية الحدودية لمراقبة الجودة بمطار 8 ماي 1945 (عين أرنات)** (36.1780, 5.3250).
  3. **المفتشية الإقليمية للتجارة بالعلمة** (36.1528, 5.6908).
  4. **المفتشية الإقليمية للتجارة بعين ولمان** (35.9189, 5.2975).
  5. **المفتشية الإقليمية للتجارة ببوقاعة** (36.3325, 5.0886).
  6. **الملحقة التجارية بعين آزال** (35.8450, 5.4622).
  7. **الملحقة التجارية بعين الكبيرة** (36.3650, 5.4980).
  8. **الملحقة التجارية بعين أرنات** (36.1850, 5.3120).
* **إلغاء طلب تصوير الوجه (السيلفي) نهائياً** لحماية خصوصية الموظفين.
* **الانصراف الميداني المرن**: تسجيل الانصراف من موقع انتهاء المهام الرقابية مع تفعيل حجب التتبع فوراً خارج أوقات العمل.

### 6. معمارية التبويبات والشاشات (Navigation & UI Architecture)
* **شريط التبويبات العلوي في `AppBar`**:
  * جميع الشاشات الإدارية (`AdminScreen`, `HeadScreen`, `BureauScreen`) تعتمد شريط التبويبات العلوي (`AppBar.bottom`) مع خط مؤشر ذهبي ملكي (`#D4AF37`) عالي الوضوح.
  * تصميم متجاوب لعنوان الـ `AppBar` باستخدام `Expanded` و `Flexible` و `TextOverflow.ellipsis` لمنع أي `RenderFlex overflow` على كافة أحجام الشاشات.
* **ختم وبصمة الحقوق الإدارية (`AppFooter`)**:
  * يتم إدراج الختم داخل المحتوى القابل للتمرير (`SingleChildScrollView`) في أسفل كل تبويب بدلاً من تثبيته في `bottomNavigationBar`، مما يمنحه مظهراً احترافياً وأنيقاً ويوفر مساحة الشاشة الحية كاملة.

---

## 🔑 الحسابات الرسمية والصلاحيات (Official Credentials)

| المستخدم (Username) | كلمة المرور (Password) | الدور (Role) | الصلاحية والوصف |
| :--- | :--- | :--- | :--- |
| `tracker_admin` | `admin123` | `admin` | **مدير النظام التقني**: إدارة الحسابات، ضبط فترة التسامح الصباحية، مراقبة الخوادم |
| `directeur` | `directeur123` | `director` | **المدير الولائي (الآمر بالصرف)**: لوحة القيادة، الخريطة الحية، الفصل في الاستفسارات، ضبط فترة التسامح، تصدير PDF |
| `chef_concurrence` | `chef123` | `head_of_department` | **رئيس مصلحة المنافسة والتحقيقات الاقتصادية**: أوامر المهمة، اعتماد المعاينات والمحاضر |
| `chef_consommation` | `chef123` | `head_of_department` | **رئيس مصلحة حماية المستهلك وقمع الغش**: توجيه الفرق الرقابية، متابعة المحاضر والمحجوزات |
| `chef_administration` | `chef123` | `head_of_department` | **رئيس مصلحة الإدارة والوسائل**: الوصاية على مكتب المستخدمين، تسيير الوسائل العامة والموارد البشرية |
| `bureau_user` | `bureau123` | `bureau_chief` | **رئيس مكتب المستخدمين**: متابعة تراكم ساعات التأخر، إصدار الاستفسارات، وتنفيذ قرارات خصم المدير على الرواتب |
| `emp.1` إلى `emp.267` | `chef123` | `inspector` | **حسابات المفتشين الميدانيين (267 موظف)**: تسجيل الحضور والانصراف بالـ GPS، تسجيل المعاينات، والرد على الاستفسارات |

---

## 🔄 مسار العمل الميداني والانضباطي (Inspection & Discipline Workflow)

```
1. تسجيل الحضور الصباحي
   └─► بصمة GPS ذكية بمحيط المقر أو المفتشية ──► مقارنة التوقيت مع فترة التسامح (مثلاً 08:45)
2. حساب التأخر التراكمي
   └─► النظام يجمع الدقائق والساعات الزائدة شهرياً ويقترح الخصم وفق الأمر 06-03
3. توجيه الاستفسار الكتابي
   └─► مكتب المستخدمين أو المدير يصدر استفساراً رسمياً مطبوعاً ──► إشعار فوري للموظف
4. رد الموظف خلال 48 ساعة
   └─► المفتش يدخل تبريراته عبر التطبيق ──► يرفع الملف مباشرة للمدير
5. القرار السيادي للمدير الولائي
   └─► مراجعة التبرير + أدلة الـ GPS ──► (قبول وتبرير / تنبيه إداري / قرار خصم نافذ)
6. التنفيذ المالي
   └─► مكتب المستخدمين يسجل الخصم كمنفذ على كشف الراتب دون حق الاعتراض
```

---

## 🌐 بنية الاستضافة السحابية على Render (Cloud Architecture)

| الخدمة السحابية (Service) | نوع الخدمة في Render | الرابط الحي (Live URL) | المستودع المرتبط (Repository) | الدور والوظيفة |
| :--- | :--- | :--- | :--- | :--- |
| **واجهة الويب (Web App)** | `Static Site` | `https://dcw-setif-tracker.onrender.com` | `DCW-SETIF-TRACKER` | واجهة تطبيق Flutter Web فائقة السرعة مع الأيقونة الرسمية الذهبية وCDN عالمي |
| **خادم البيانات (Backend API)** | `Web Service` (Node.js) | `https://drh-setif-api.onrender.com/api` | `DCW-SETIF-BACKEND` | محرك الحسابات، مسار الاستفسارات، البصمة الجغرافية، والاتصال بقاعدة بيانات PostgreSQL السحابية |

### 📌 رابط الـ Deploy Hook المباشر:
```powershell
curl.exe -X POST "https://api.render.com/deploy/srv-dairbhdg1s2s738fig10?key=GLgWofYjMBE"
```

---

## 🛠️ أوامر الصيانة والبناء (Build Commands)

```powershell
# بناء تطبيق الأندرويد للإنتاج:
cd "c:\Users\ASUS 2\Nouveau dossier\DCW-SETIF-TRACKER"
flutter build apk --release

# بناء تطبيق الويب:
flutter build web --release --base-href "/"

# نسخ حزمة الويب إلى مجلد الخادم:
Copy-Item -Recurse -Force build\web\* "..\DCW-SETIF-BACKEND\public\"

# فحص سلامة كود فلاتر واختبار الشاشات:
flutter test test/all_screens_test.dart
flutter analyze

# دفع التحديثات إلى GitHub:
git add .
git commit -m "feat/docs: update architecture, tab navigation, and comprehensive docs"
git push origin main
```
