# 🏛️ DCW-SETIF — دليل الوكيل والمطور (AGENTS.md)
### منصة الرقابة والتفتيش الميداني — مديرية التجارة الداخلية وضبط السوق الوطنية لولاية سطيف

> **تاريخ آخر تحديث**: 22 سبتمبر 2026 (الجلسة الصباحية)
> **حالة المنظومة**: ✅ مكتملة ومنشورة — تم القضاء النهائي على تداخل النصوص فوق الرسومات والكتابة في لوحة المدير للهواتف الذكية (Responsive Overlap Fix)، وتوسيع العداد الدائري للامتثال، وتطوير مخطط الأعمدة اليومي المتحرك، وربط استعلام التطور اليومي بالفترات المخصصة في السيرفر.
> **مستودع التطبيق (Flutter Web & Mobile)**: [DCW-SETIF-TRACKER](https://github.com/matique2026ai-ux/DCW-SETIF-TRACKER) (الفرع الحالي: `main`)
> **مستودع الخادم (Node.js/Express/PostgreSQL)**: [DCW-SETIF-BACKEND](https://github.com/matique2026ai-ux/DCW-SETIF-BACKEND) (الفرع الحالي: `main`)
> **رابط المنصة الحية (Render Web App)**: [dcw-setif-tracker.onrender.com](https://dcw-setif-tracker.onrender.com)
> **السيرفر السحابي الحي (Render Backend API)**: `https://drh-setif-api.onrender.com/api`
> **عداد إصدارات APK للتطبيقات الميدانية**: الإصدار الحالي المبني والمُسلَّم: `v1.0.4+5` | الإصدار القادم: `v1.0.5+6`

---

## 📌 ما تم إنجازه في جلسة 22 سبتمبر 2026 — الصباح (القضاء التام على تداخل النصوص وتجاوب الهواتف):

1. **📱 القضاء التام على تداخل النصوص والرسومات في الهاتف (Mobile Responsive Overlap Elimination):**
   * **العداد الدائري للامتثال القانوني (`_ComplianceRingsPainter`)**: تكبير القطر إلى `165x165`، وتعديل سماكة الحلقات إلى `7.5` و `5.5`، وإحاطة النسبة ونص "امتثال تجاري" بـ `FittedBox` ليمنع ملامسة أو تداخل النص مع المسار الدائري تحت أي تكبير خط في الهاتف.
   * **مخطط الأعمدة اليومي المزدوج (`_buildAnimatedDailyTrendChart`)**: زيادة ارتفاع الحاوية إلى `215px`، تقليص حد الارتفاع للأعمدة إلى `105px` كحد أقصى، تغليف نصوص التواريخ بـ `FittedBox`، وإضافة تمرير أفقي ربيعي سلس `SingleChildScrollView` عند ضيق الشاشة في الموبايل لمنع تراكب التواريخ.
   * **بطاقات الـ KPI الرئيسية الأربعة (`_buildHeroMetricCard`)**: إحاطة الأرقام الكبيرة بـ `Flexible` و `FittedBox(fit: BoxFit.scaleDown)` وضبط نسبة الأبعاد `childAspectRatio: 2.7` لمنع تداخل كلمة "تفاصيل" مع أرقام المحجوزات المليونية.
   * **شبكة الإجراءات الردعية (`_buildSanctionTile`)**: تعديل `childAspectRatio` إلى `1.85` واستخدام `FittedBox` لمنع انضغاط النصوص عمودياً.
   * **شريط الترويسة العلوي (`_buildExecutiveHeader`)**: جعل العنوان وشارة البث المباشر مرنة عبر `Flexible` و `ConstrainedBox`.

2. **⚙️ ربط مخطط التطور اليومي في السيرفر بالفترة الزمنية المحددة (`dashboard.js`):**
   * تحويل استعلام `dailyTrend` ليتكيف تلقائياً مع الفترات المختارة (`week`, `month`, `custom_date`, `cumulative`) بدلاً من تثبيته على آخر 7 أيام حالية فقط.

3. **✨ فحص الجودة وبناء الويب:**
   * اجتياز `flutter analyze` بنتيجة تامة **0 Errors / 0 Warnings**.
   * بناء نسخة الويب بنجاح: `flutter build web --release --no-tree-shake-icons`.

---

## 📌 ما تم إنجازه في جلسة 21 سبتمبر 2026 — المساء (إعادة هيكلة لوحة المدير ومعالجة الفراغات):

1. **🎨 استئصال آخر بقايا اللون الأزرق (Zero Blue — Final Sweep):**
   * اكتشاف وإزالة `Color(0xFF38BDF8)` (Cyan) في `_buildMiniPill` للمحاضر القضائية في `_buildDepartmentCard`.
   * الاستبدال: **الأخضر الزمردي** `#10B981` — المنصة الآن خالية 100% من أي أثر للأزرق أو السماوي.

2. **🃏 إعادة هيكلة بطاقة الحصيلة التراكمية (Cumulative Card Redesign):**
   * **المشكلة**: `GridView` مع `childAspectRatio: 2.4` كان يُنتج خلايا ضخمة بفراغات غير مفيدة.
   * **الحل المطبَّق**:
     * استبدال الشبكة بـ **صفوف أفقية مضغوطة (2 مقاييس في كل صف)** بدون أي فراغات.
     * إضافة **قائمة أحدث المعاينات الميدانية الحية** (حتى 6 معاينات من `recentVisits`) مباشرة داخل البطاقة، تعرض: اسم المحل، نوع النشاط، حالة المطابقة (مطابق/مخالفة) بلون أخضر/أحمر، والتاريخ.
     * إضافة `_buildCumMetricTile` كـ helper widget مستقل مع أنيمايشن Fade-in.

3. **📱 بناء APK v1.0.4+5 الرسمي (72.9MB):**
   * ترقية `pubspec.yaml`: `version: 1.0.3+4` → `version: 1.0.4+5`
   * البناء: `flutter build apk --release --no-tree-shake-icons` ✅
   * المسار: `build/app/outputs/flutter-apk/app-release.apk`

4. **✨ معايير الجودة الصارمة:**
   * `flutter analyze` → **0 Errors / 0 Warnings** محقق عبر جميع التعديلات.
   * بناء الويب ونشره على Render بنجاح (Deploy: `dep-daok4ro473hc73al4aug`).

---

## 🔧 الملفات المعدَّلة في هذه الجلسة:

| الملف | التعديل |
|-------|---------|
| `lib/screens/director/director_analytics_tab.dart` | إعادة هيكلة شاملة: بطاقات KPI، إزالة اللون الأزرق، بطاقة الحصيلة التراكمية الجديدة، قائمة المعاينات الحية |
| `lib/services/pdf_report_service.dart` | استبدال `pw.GridView` بـ `pw.Table` في تقرير PDF |
| `pubspec.yaml` | ترقية الإصدار: `1.0.3+4` → `1.0.4+5` |

---

## 📌 ما تم إنجازه وتطبيقه في جلسة 21 سبتمبر 2026 (الرسوم البيانية المتحركة والتحليلات السيادية الحية):

1. **🎨 إعادة هيكلة بطاقات لوحة المدير واستئصال اللون الأزرق نهائياً (Card Hierarchy & Zero Blue Palette):**
   * تقسيم الـ 8 بطاقات السابقة إلى مستويين متناسقين هندسياً:
     * **المستوى الأول (4 ركائز استراتيجية)**: معاينات الرقابة (ذهب ملكي `#D4AF37`)، المخالفات والمحاضر (أحمر تنفيذي `#EF4444`)، قيمة المحجوزات (عنبري `#F59E0B`)، نسبة الامتثال التجاري (أخضر زمردي `#10B981`) بارتفاع متناسق ومدمج.
     * **المستوى الثاني (4 إجراءات ميدانية وقضائية مصغرة)**: الجاهزية والانتشار، المتابعات القضائية، اقتراح الغلق الإداري، واقتطاع العينات، بتصميم مدمج أنيق وواضح.
   * إزالة أي أثر للون الأزرق أو السماوي عبر كافة الواجهات والرسوم البيانية والمخططات الدائرية والأشرطة.

2. **📄 إصلاح قالب التقرير الإحصائي السيادي (Sovereign PDF Fix):**
   * استبدال `pw.GridView` التي كانت تسبب تمدداً رأسياً مشوهاً للبطاقات في ملف الـ PDF بجدول رسمي سيادي متقن من صفين و4 أعمدة (`pw.Table`) بخلفية بيضاء نقية وإطار كهرماني رفيع وخطوط واضحة عالية التباين.

3. **📈 الرسوم البيانية المتحركة الرشيقة والفائقة السلاسة (GPU-Accelerated 60 FPS Charts):**
   * **مخطط التطور اليومي المزدوج بالأعمدة المتحركة (`Dual-Bar Animated Chart`)**: مقارنة فورية بين حجم المعاينات الميدانية (باللون الذهبي الملكي `#D4AF37`) والمخالفات المكتشفة (Red `#EF4444`) مع ميزة `Hover/Tooltip` تعرض تفاصيل المعاينات والمخالفات وقيم المحجوزات المحررة بالدينار الجزائري.
   * **مخطط القطاعات والامتثال القانوني الدائري المتحرك (`Concentric Rings Gauge`)**: رسم بياني دائري متحد المركز بمحرك `CustomPainter` لحساب نسبة الامتثال التجاري للأنشطة وفق القانونين **09-03** و **04-02** ونسبة الجاهزية الميدانية.
   * **مؤشرات الأنشطة والقطاعات التجارية بالأشرطة الأفقية المتحركة**: توزيع المعاينات والمخالفات حسب الأنشطة بألوان الذهبي والزمردي.

4. **✨ معايير الجودة الصارمة والنشر الفوري:**
   * اجتياز فحص `flutter analyze` بنتيجة تامة **`0 Errors / 0 Warnings`**.
   * بناء نسخة الويب بنجاح ودفعها على `main` وإطلاق النشر السحابي التلقائي على Render.

1. **📈 الرسوم البيانية المتحركة الرشيقة والفائقة السلاسة (GPU-Accelerated 60 FPS Charts):**
   * **مخطط التطور اليومي المزدوج بالأعمدة المتحركة (`Dual-Bar Animated Chart`)**: مقارنة فورية بين حجم المعاينات الميدانية (Cyan `#38BDF8`) والمخالفات المكتشفة (Red `#EF4444`) مع ميزة `Hover/Tooltip` تعرض تفاصيل المعاينات والمخالفات وقيم المحجوزات المحررة بالدينار الجزائري.
   * **مخطط القطاعات والامتثال القانوني الدائري المتحرك (`Concentric Rings Gauge`)**: رسم بياني دائري متحد المركز بمحرك `CustomPainter` و `TweenAnimationBuilder` لحساب نسبة الامتثال التجاري للأنشطة وفق القانونين **09-03** لحماية المستهلك و **04-02** للممارسات التجارية ونسبة الجاهزية الميدانية.
   * **مؤشرات الأنشطة والقطاعات التجارية بالأشرطة الأفقية المتحركة**: حساب ديناميكي لتوزيع المعاينات والمخالفات حسب الأنشطة (مواد غذائية، إطعام سريع، تجارة جملة، مخابز، قصابة ولحوم) مع تنبيهات ونسب المخالفات لكل قطاع.

2. **🔴 الربط الحي الصارم بقاعدة البيانات السحابية الحية (Live PostgreSQL REST Data):**
   * دعم فلترة متطورة للمدى الزمني عبر استعلامات مخصصة (`period`: `today`, `week`, `month`, `cumulative`, `custom`).
   * إزالة أي بيانات وهمية أو أصفار جامدة وربط التدخلات الحقيقية للمفتشين وإحصائيات المفتشيات الثمانية بولاية سطيف.

3. **📄 التصدير الفوري لتقرير الإحصائيات الرقابية الرسمي (Official Sovereign PDF):**
   * تفعيل زر التصدير في شريط القيادة العلوي لربطه مباشرة بمسار `/api/reports/inspection-summary` وطباعة تقرير معتمد مؤشر ومختوم يحمل كود التحقق السيادي ومؤشرات KPIs.

4. **✨ معايير الجودة الصارمة والنشر الفوري:**
   * اجتياز فحص `flutter analyze` بنتيجة تامة **`0 Errors / 0 Warnings`**.
   * بناء نسخة الويب بنجاح ودفعها على `main` وإطلاق النشر السحابي التلقائي على Render.

1. **📊 تقرير الإحصائيات الرقابية الشامل وأرشيف الـ GPS (Comprehensive Inspection & GPS Report):**
   * **الخادم (`DCW-SETIF-BACKEND`)**: تزويد مسار `/api/reports/inspection-summary` بقدرة تجميع بيانات المعاينات والحضور الميداني مع خطوط الطول والعرض وقيم المحجوزات والمخالفات ومعدلات التأشير الإداري بدعم PostgreSQL و SQLite.
   * **الواجهة وطباعة PDF (`DCW-SETIF-TRACKER`)**: تطوير `PdfReportService.generateAndPrintInspectionSummaryReport` لتوليد تقارير رسمية مؤشرة بصفحتين تحتوي على مؤشرات KPIs، جدول المصالح، التطور اليومي، ترتيب المفتشين، وأرشيف البصمة الجغرافية الموثق، مع زر تشغيل فوري في لوحة المدير (`director_screen.dart`).

2. **🚫 الاستئصال الجذري للواجهات والبيانات الوهمية (Elimination of Fake Mocks & Parc Auto):**
   * استبدال التبويب الوهمي لحظر السيارات والوقود بـ **"المقرات والبصمة الجغرافية"** المعروضة حواضرها ومفتشياتها الثمانية من `AppConstants.defaultInspectorates` مع حسابات المسافة الحقيقية (Haversine).

3. **🔗 الربط المنطقي الصارم بين الموظفين الفعليين وحسابات المستخدمين (`seedEmployees` & `seedUsers`):**
   * ربط حسابات المستخدمين (`inspecteur`, `kamel_kribaa`, `djamel_lounis`, ...) مباشرة بجدول الموظفين الفعليين `Employes` لمنع الأخطاء وحماية هويات الموظفين الحقيقيين.

4. **📍 القفل الصارم للبصمة الجغرافية وضوابط الهاتف (GPS & Device Security):**
   * اشتراط توفر إشارة GPS حقيقية ومطابقة النطاق الجغرافي للمقرات الـ 8 أو وجود أمر مهمة تفتيشية مسند رسمياً.
   * ربط الدخول بحقل `DeviceId` وحماية النظام من محاولات التلاعب.

5. **🛡️ خطة العمل المنطقية ومعيار الجودة الشامل (Master Verification Standard):**
   * التزام تام بدورة فحص ثلاثية تضمن **`0 Errors / 0 Warnings`** في `flutter analyze` قبل رفع أي تحديث.
   * إحاطة جميع المسارات بصمامات أمان لتفادي استثناءات `Null` و `Undefined`.

6. **✨ توحيد الهوية البصرية الملكية (Golden Focus Styling & APK v1.0.3+4):**
   * ضبط مؤشر الكتابة `cursorColor`، ولون التحديد `selectionColor`، وتأصيل النص المرتفع `floatingLabelStyle` باللون الذهبي الملكي (`0xFFD4AF37`) عبر الثيم العام `AppTheme` وشاشة الدخول `login_screen.dart`، وترقية حزمة الـ APK رسمياً إلى `v1.0.3+4`.

7. **🛡️ منظومة الدرع المزدوج لحماية الحسابات (Dual Shield Hardware & Web Master PIN):**
   * **تطبيق الهاتف (Mobile)**: قفل عتادي صارم برقم البصمة الرقمية `DeviceId` للمدير التقني `tracker_admin` والمفتشين؛ منع دخول أي هاتف آخر نهائياً.
   * **متصفح الويب (Web Browser)**: اشتراط رمز الأمان السري الشخصي (`Master PIN` الافتراضي: `202600`) للدخول كمدير نظام من أي متصفح، مما يتيح المرونة الكاملة للأدمن مع استحالة الاختراق.

8. 📜 **منظومة التحقق والاعتماد الرقمي المانعة للتزوير (Anti-Fraud Digital Verification):**
   * **رابط التحقق السيادي السحابي**: مسار فوري عالي السرعة (`/verify`) مشفر بخوارزمية التوقيع الرقمي **`SHA-256`**.
   * **إثبات عدم التزوير**: لا يقبل التعديل أو التزييف؛ عند مسح الكود بأي كاميرا أو جهاز، يتم استرجاع شهادة الاعتماد الرسمية من السيرفر الحي وتأكيد البصمة الجغرافية، توقيت التسجيل، اسم الموظف، والختم المشفر.

9. ⚖️ **منظومة الانضباط والقرارات السيادية والتقارير الدورية (Discipline, Inquiries & Sovereign PDF Reports):**
   * **إصلاح المسار الحي (`/api/inquiries`)**: مطابقة حقول `ReplyDate` و `DecisionDate` بقاعدة بيانات PostgreSQL السحابية، وربط ردود ومرفقات الموظفين في قاعدة البيانات بشكل فوري.
   * **تبويب الانضباط والاستفسارات (Discipline & Explications)**: استبدال مسمى "الخصم" بالمسمى الإداري الرسمي، مع تزويد التبويب بشعار تنبيهي حي (Alert Badge) يتغير لونه وينبض بالأحمر عند ورود رد رسمي من المفتش بانتظار قرار المدير، وشريط سيادي علوي عاجل.
   * **شريط الفلترة التنفيذي والبحث الذكي**: فلترة أرشيف السوابق زمنياً (الكل، اليوم، هذا الأسبوع، هذا الشهر) وحسب نوع القرار (مبرر مقبول، إنذار رسمي، خصم مالي) وبحث فوري بالاسم والرتبة والمصلحة.
   * **محضر وقرارات الانضباط الرسمي (PDF)**: دمج `PdfReportService.generateAndPrintDisciplineReport` لتوليد محضر سيادي معتمد يحمل ترويسة الجمهورية ومؤشرات KPIs وجدول القرارات وختم السيد المدير الولائي الآمر بالصرف.

---

## ⚠️ تعليمات صارمة غير قابلة للنقاش للوكلاء والمطورين اللاحقين (STRICT AGENT DIRECTIVES)

1. **🚫 منع توليد حزمة الأندرويد APK نهائياً والبناء الإلزامي للويب:**
   * **ممنوع قطعياً تشغيل `flutter build apk` أو توليد أي ملف بصيغة `.apk` تحت أي ظرف، إلا بطلب كتابي صريح ومباشر من المستخدم** (التوجيه الصريح: *"لا تولد تطبيق ابيكي فقط حتى امرك"*).
   * **إلزامي وصارم**: أي تعديل على كود الواجهة الأمامية (Frontend) يجب أن يعقبه فوراً وقبل أي `git push` إعادة بناء كاملة لنسخة الويب عبر:
     `flutter build web --release --no-tree-shake-icons`
     ثم عمل `git add build/web` و `git commit` ودفعها (`push`) إلى GitHub مع استدعاء خطاف Render Deploy Hook.
2. **🛡️ حساب مدير النظام التقني (tracker_admin) ممنوع اللمس:**
   * الحساب `tracker_admin` (Role 5) **محمي وممنوع اللمس نهائياً**؛ لا يتم حذفه، لا تعديل صلاحياته، ولا تفريغه.
3. **🚫 منع الأكواد والواجهات الوهمية (No Fake Mocks / No Hallucinated Features):**
   * **ممنوع إنشاء أي زر أو شاشة أو بيانات شكلية لا ترتبط بجدول حقيقي في قاعدة البيانات ومسار حقيقي في السيرفر**.
   * كل ميزة يجب أن تكون شغالة فعلياً ومترابطة منطقياً بين قاعدة البيانات والخادم والواجهة.
4. **🌿 إدارة الفروع والتحديث السحابي:**
   * التحديثات تدمج وتدفع دائماً على فرع `main` لكلا المستودعين.
   * يتم تفعيل الـ Webhook الخاص بـ Render لنشر النسخة الحية بعد كل بناء للويب.
5. **🔍 معايير الجودة الصارمة:**
   * الحفاظ الدائم على **`0 Errors / 0 Warnings`** في `flutter analyze`.

---

## 🔑 الحسابات المعتمدة النشطة في قاعدة البيانات:

| المستخدم (Username) | كلمة المرور (Password) | الدور (Role) | الصفة الإدارية والارتباط |
| :--- | :--- | :--- | :--- |
| **`tracker_admin`** | `admin123` | `admin` (Role 5) | **مدير النظام التقني (محفوظ 100% — ممنوع اللمس)** |
| **`directeur`** | `directeur123` | `director` (Role 1) | **المدير الولائي (الآمر بالصرف الوحيد)** |
| **`chef_administration`** | `chef123` | `head_of_department` (Role 2) | **رئيس مصلحة الإدارة والوسائل** |
| **`chef_consommation`** | `chef123` | `head_of_department` (Role 2) | **رئيس مصلحة حماية المستهلك وقمع الغش** |
| **`chef_concurrence`** | `chef123` | `head_of_department` (Role 2) | **رئيس مصلحة المنافسة والتحقيقات الاقتصادية** |
| **`bureau_user`** / **`chef_bureau`** | `bureau123` | `bureau_chief` (Role 3) | **رئيس مكتب المستخدمين** |
| **`inspecteur`** | `chef123` | `inspector` (Role 4) | **مفتش رقابة مرتبط بالموظف الفعلي (كمال كريبع)** |
| **`kamel_kribaa`** | `chef123` | `inspector` (Role 4) | **مفتش رئيسي لقمع الغش (مرتبط بالموظف ID: 2)** |
| **`djamel_lounis`** | `chef123` | `inspector` (Role 4) | **محقق رئيسي للمنافسة (مرتبط بالموظف ID: 3)** |

---

## 🏛️ شبكة المقرات والمفتشيات الثمانية المعتمدة للبصمة الجغرافية (AppConstants.defaultInspectorates):

1. **المقر الرئيسي للمديرية الولائية**: سطيف — حي المعبودة (`36.1900575, 5.3990134` — نصف القطر: 250م).
2. **المفتشية الحدودية لمراقبة الجودة**: مطار 8 ماي 1945 الدولي، عين أرنات (`36.1781, 5.3247` — نصف القطر: 1200م).
3. **المفتشية الإقليمية للتجارة بالعلمة**: وسط العلمة (`36.1554, 5.6908` — نصف القطر: 1200م).
4. **المفتشية الإقليمية للتجارة بعين ولمان**: القطاع الجنوبي (`35.9189, 5.2978` — نصف القطر: 1200م).
5. **المفتشية الإقليمية للتجارة ببوقاعة**: القطاع الشمالي والغربي (`36.3325, 5.0886` — نصف القطر: 1200م).
6. **الملحقة التجارية بعين آزال**: جنوب الولاية (`35.8686, 5.4667` — نصف القطر: 1000م).
7. **الملحقة التجارية بعين الكبيرة**: شمال الولاية (`36.3639, 5.5003` — نصف القطر: 1000م).
8. **الملحقة التجارية بعين أرنات**: غرب الولاية (`36.1833, 5.3167` — نصف القطر: 1000م).

---

## 🔄 مسار العمل المترابط الحقيقي (Integrated Real-Time Workflow):

```
1. المفتش الميداني (Inspecteur):
   └─► يسجل حضوره الصباحي بالـ GPS ضمن أحد المقرات الـ 8 أو بناءً على أمر مهمة معتمد 
   └─► ينفذ الزيارات الميدانية ويوثق المخالفات والعينات والمحجوزات بالصور والإحداثيات.
2. رئيس المصلحة المعنية (قمع الغش / المنافسة):
   └─► يستقبل المعاينات في لوحته فورياً، يراجع المحاضر ويصادق عليها.
3. رئيس مصلحة الإدارة والوسائل (Administration & Moyens):
   └─► يتابع حضور وانضباط كافة موظفي المديرية، المقرات الثمانية، التأخرات التراكمية، ومقترحات الخصم القانونية وفق الأمر 06-03.
4. المدير الولائي (Directeur - Ordonnateur):
   └─► يشرف عبر الخريطة الحية على تموضع الفرق، ويعتمد القرارات الإدارية والمالية السيادية.
```

---

## 🌐 روابط وخطافات النشر السحابي (Render Deploy Hooks):

* **رابط إعادة نشر سيرفر الـ API والبيانات (Backend Service Hook - drh-setif-api):**
  ```powershell
  curl.exe -X POST "https://api.render.com/deploy/srv-daiqf167bikc739mt7lg?key=B2bktUL4jYM"
  ```
* **رابط إعادة نشر واجهة الويب (Frontend Web App Hook - DCW-SETIF-TRACKER):**
  ```powershell
  curl.exe -X POST "https://api.render.com/deploy/srv-dairbhdg1s2s738fig10?key=GLgWofYjMBE"
  ```
* **رابط فحص الحالة والصحة الحية:**
  `https://drh-setif-api.onrender.com/api/health`
